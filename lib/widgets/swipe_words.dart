import 'package:flutter/material.dart';

class SwipeWords extends StatelessWidget {
  const SwipeWords({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}